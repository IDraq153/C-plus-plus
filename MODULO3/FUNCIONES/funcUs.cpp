#include <iostream>
using std::cout;

int main() {
    double x = 3.9;
    double y = 15;
    double z;

    //FUNCIONES MAS USADAS
    /*z = std::max(x,y);
    z = std::min(x,y);
    z = std::pow(x,y);
    z = std::sqrt(9);
    z = std::round(x);
    z = std::ceil(x);
    z = std::floor(x);
    z = std::abs(-15);
    cout << z;*/

    return 0;
}
